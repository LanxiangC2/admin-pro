import { it, expect, describe } from 'vitest';

describe('utils test', () => {
    it('100% test', () => {
        expect(1).toBe(1);
    });
    it('add', () => {
        expect(1 + 1).toBe(2);
    });
});
